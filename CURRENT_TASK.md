# EduTech — Current Task

> هذا الملف متغير ويجب تحديثه في نهاية كل جلسة.  
> **آخر تحديث:** 2026-09-18  
> **الحالة:** نقطة أساس جديدة (Baseline) بعد نقل المشروع إلى VS Code — working tree نظيف، والمتبقي هو الفحص البصري والتفاعلي فقط.

## المهمة الحالية

إكمال المرحلة الأولى من تطوير **أدوات الإدراج داخل محرر البطاقة** مع الحفاظ على بساطة Word/Google Docs، والتركيز أولًا على تنظيم شريط الإدراج ونافذة المعادلات دون إعادة بناء المحرر أو المحرك الرياضي.

## ما تم حتى الآن

- أعيد تنظيم شريط الإدراج في `RichTextEditor.tsx` إلى ثلاث مجموعات:
  - **عناصر الدرس:** معادلة، جدول، صورة.
  - **استيراد واستخراج:** استيراد DOCX/PDF/HTML/TXT، واستخراج OCR.
  - **مساعدة:** المساعد الذكي.
- حُسّن تدفق `EquationEditorModal.tsx` إلى:
  1. اختر البنية أو الرمز.
  2. اكتب وعدّل المعادلة.
  3. راجع ثم أدرج في البطاقة.
- أصبحت فئة الكسور والجذور مفتوحة افتراضيًا، وأصبحت فئات الأدوات أوضح وأكبر.
- وُضحت حالتا المعادلة: داخل السطر أو سطر مستقل.
- وُضح مسار لصق Word أو تحرير LaTeX/MathML.

## الملفات المعنية بالمهمة

- `src/modules/editor/components/RichTextEditor.tsx` — موجود ضمن الـ baseline الحالي، ويتضمن تعديلات شريط الإدراج.
- `src/modules/editor/components/EquationEditorModal.tsx` — موجود ضمن الـ baseline الحالي، ويتضمن تعديلات تدفق نافذة المعادلات.

لا توجد تعديلات غير ملتزمة حاليًا؛ working tree نظيف قبل أي جلسة جديدة.

توجد ملفات `edutech-preview-*.tar.gz` غير متتبعة مسبقًا؛ لا تضفها إلى Git.

## ما تم التحقق منه

نجحت على مساحة العمل الحالية بتاريخ 2026-09-18:

- `npm run lint`
- `npm test`
- `npm run build`
- `git diff --check`

الاختبارات تشمل `lessonLifecycleAudit` ونجحت 5/5. يوجد تحذير Vite فقط عن chunks أكبر من 1500 kB.

## ما لم يتم بعد

- لم يُفحص شريط الإدراج ونافذة المعادلات بصريًا أو تفاعليًا بعد.
- لم يُختبرا على Desktop/Tablet/Mobile أو مع overflow أفقي وRTL.
- لم تُجرّب دورة فعلية: إدراج معادلة inline/block، جدول، صورة، ملف، OCR، ثم حفظ وسابق/تالي.
- لم تُفحص القوائم المنسدلة للتأكد من عدم ظهورها خلف المحرر.
- المتبقي من المهمة الحالية هو الفحص البصري والتفاعلي فقط، وليس إعادة برمجة أو إعادة تصميم من الصفر.

## الخطوة التالية المباشرة

1. افحص `git status` و`git diff` واحفظ التعديلين الحاليين.
2. شغّل التطبيق وافتح درسًا من مستكشف المناهج ثم بطاقة من مخطط الدرس.
3. نفذ فحصًا بصريًا للشريط ونافذة المعادلات على المقاسات الثلاثة.
4. اختبر الإدراج والحفظ والتنقل والمعاينة دون فقد محتوى.
5. أصلح فقط العيوب المثبتة، ثم أعد `lint`, `test`, `build`.
6. بعد قبول بصري صريح: أنشئ commit مستقلًا، ثم push/publish فقط إذا كان ذلك مطلوبًا ومصرحًا.

## نقطة الأساس الموثوقة

- **الفرع الحالي:** `edutech-latest-2026-09-18`
- **يتتبع:** `origin/edutech-latest-2026-09-18`
- **آخر commit موثوق:** `c3397a8`
- **العنوان:** `Baseline - EduTech latest before AI agents`
- **Git:** فرع مسمى ومتتبع بالكامل، وworking tree كان نظيفًا قبل هذا التحديث.
- **آخر نسخة منشورة معروفة:** Preview version 33؛ يلزم التحقق قبل اعتبارها أحدث نشر بعد هذا الـ baseline.

## قالب تحديث نهاية الجلسة

استبدل محتوى الأقسام المتغيرة أعلاه، وسجل باختصار:

- المهمة التي نُفذت.
- الملفات المعدلة.
- نتيجة الفحص البصري.
- أوامر التحقق ونتائجها.
- commit وpush/publish إن حدثت.
- المشكلة المفتوحة الوحيدة أو الخطوة التالية الواضحة.

